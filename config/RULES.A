#RULES.A
# Note: we want this RULES file to be executed first after
#  $(TOP)/config/CONFIG, so the name RULES.A is important.

all install:: clean_made_apps

ifeq (0,${MAKELEVEL})
clean_made_apps::
	@$(RM) $(TOP)/.made.*
else
clean_made_apps::
endif

.PHONY:: clean_made_apps
